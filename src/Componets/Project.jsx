import { Link } from "react-router-dom";

const Project = () => {
    return (
        <section>
        <Link to={'/'}>
            <button>Home</button>
        </Link>
        <h1>Project</h1>
        </section>
    )
};

export default Project;